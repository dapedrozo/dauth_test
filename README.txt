Descargar el proyecto
Ejecutar el comando "npm i" para instalar los paquetes utilizados como express o nodemon por ejemplo
A continuacion correr el server con el comando "npm run dev" (esto inicializara el sever en el port 7000)
